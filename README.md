(In progress)This is the RESTful API for the Ecommcerce website.
 - Just added the GET endpoints to retrieve all the data.


---
Install all dependencies with
```
npm install
```
Start the server by running:
```
node src/index.js
```